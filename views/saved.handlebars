<div class='article-container'>
    {{#each this}}
    <div class='article'>
        <div class='title'>
            <h2>
                <a href='{{link}}'>{{title}}</a>
            </h2>
        </div>
        <br>
        <div class='image-summary'>
            <a href='{{link}}'>
                <img src='{{img}}' alt='article-image' />
            </a>
            <p class='summary'>
                {{summary}}
            </p>
        </div>
        <div id='myModal' class='modal'>
            <div class="modal-content">
                <span class="close">&times;</span>
                Title
                <input id='insert-title' type='text'>
                <br> Note
                <textarea id='insert-body'></textarea>
                <button class='submit-note' id='{{_id}}' type='submit'>Save Note</button>
            </div>
        </div>
        <div class='comment-div'>
            <button class='note' id='{{_id}}'>Note</button>
            <button class='delete' id='{{_id}}'>Delete Article</button>
        </div>
        <br>
    </div>
    {{/each}}
</div>
</div>